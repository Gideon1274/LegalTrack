# LegalTrack environment variables
#
# This project defaults to Supabase Postgres.
# Provide a real DATABASE_URL from Supabase (Project Settings -> Database -> Connection string).
#
# IMPORTANT: Never commit your real .env file or secrets.

# --- Django ---
# Set a stable secret key for development/production.
DJANGO_SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_VALUE

# --- Database provider ---
# supabase (default) or sqlite (explicit opt-in, e.g. tests/offline dev)
LEGALTRACK_DB_PROVIDER=supabase

# When using Supabase, you can optionally allow a local SQLite fallback in
# development if the Supabase host can't be resolved on your current network.
# This is OFF by default to avoid accidentally using SQLite when you expect
# Supabase.
# LEGALTRACK_ALLOW_SQLITE_FALLBACK=true

# Supabase Postgres (required when LEGALTRACK_DB_PROVIDER=supabase)
# Use the exact URI from Supabase. Ensure sslmode=require.
# Examples:
#   postgresql://postgres:YOUR_PASSWORD@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
#   postgresql://postgres.<project-ref>:YOUR_PASSWORD@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=postgresql://postgres:CHANGE_ME_PASSWORD@CHANGE_ME_HOST:5432/postgres?sslmode=require

# Optional overrides if you prefer not to use DATABASE_URL parsing
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=...
# DB_HOST=...
# DB_PORT=5432

# --- Optional feature toggles (defaults are set in settings.py) ---
# LEGALTRACK_SEND_EMAILS=true
# LEGALTRACK_SHOW_ACTIVATION_LINK=true
